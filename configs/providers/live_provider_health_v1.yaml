profile_id: live_provider_health_v1
description: Reachability proof config for a local OpenAI-compatible server, with room for remote providers later.
targets:
  - target_id: local_openai
    provider: local_openai
    client_provider: openai_compat
    model: local-openai-proof
    api_mode: auto
    base_url: http://127.0.0.1:0/v1
    api_key_env: OPENAI_API_KEY
    use_dummy_key: true
    dummy_api_key: local-openai-proof
    launch_fixture_server: true
    timeout_s: 8
    max_tokens: 80
    temperature: 0.0
    structured_probe: true
    expected_usage: true
    expected_latency: true
    notes: Builtin local OpenAI-compatible proof server used when no external provider is configured.
